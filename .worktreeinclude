# Hydration config — generated by Cuttlefish
#
# Directories listed here are CoW-cloned into new worktrees.
# Built-in artifact dirs (node_modules, target, etc.) are always
# included unless excluded with '!' prefix.
# Add project-specific directories below.
